import { createChatBotMessage } from 'react-chatbot-kit';

const config = {
  botName: "Explora",
  initialMessages: [createChatBotMessage("Greetings, what do you like to watch?")],
};

export default config;
